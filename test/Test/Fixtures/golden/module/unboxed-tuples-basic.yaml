extensions: [UnboxedTuples]
input: |
  {-# LANGUAGE UnboxedTuples #-}
  module UnboxedTuplesBasic where
  x = (# 1, 2 #)
  f (# a, b #) = a
  h :: (# Int, Int #) -> (# Int, Int #)
  h t = t
ast: Module {name = "UnboxedTuplesBasic", languagePragmas = [EnableExtension UnboxedTuples], decls = [DeclValue (PatternBind NoMultiplicityTag PVar "x" UnguardedRhs (ETupleUnboxed [EInt 1 TInteger, EInt 2 TInteger])), DeclValue (FunctionBind "f" [Match {headForm = Prefix, pats = [PTupleUnboxed [PVar "a", PVar "b"]], rhs = UnguardedRhs (EVar "a")}]), DeclTypeSig {names = ["h"], type = TFun ArrowUnrestricted (TTupleUnboxed [TCon "Int", TCon "Int"]) (TTupleUnboxed [TCon "Int", TCon "Int"])}, DeclValue (FunctionBind "h" [Match {headForm = Prefix, pats = [PVar "t"], rhs = UnguardedRhs (EVar "t")}])]}
status: pass
