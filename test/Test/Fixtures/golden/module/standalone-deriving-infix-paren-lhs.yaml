extensions: []
input: |
  deriving instance ((C) '()) :+ ()
ast: |
  Module {decls = [DeclStandaloneDeriving (StandaloneDerivingDecl {head = TInfix (TParen (TApp (TParen (TCon "C")) (TTuplePromoted []))) ":+" (TTuple [])})]}
status: pass
