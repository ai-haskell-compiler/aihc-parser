extensions: []
input: |
  module M where
  class xs `Infix` ys
  instance as `Infix` bs
ast: |
  Module {name = "M", decls = [DeclClass (ClassDecl {headForm = TypeHeadInfix, name = "Infix", params = [TyVarBinder {name = "xs"}, TyVarBinder {name = "ys"}]}), DeclInstance (InstanceDecl {head = TInfix (TVar "as") "Infix" (TVar "bs")})]}
status: pass
