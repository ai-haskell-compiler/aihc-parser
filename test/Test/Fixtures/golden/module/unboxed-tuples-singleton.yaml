extensions: [UnboxedTuples]
input: |
  {-# LANGUAGE UnboxedTuples #-}
  module UnboxedTuplesSingleton where
  x = (# 1 #)
  f (# y #) = y
ast: Module {name = "UnboxedTuplesSingleton", languagePragmas = [EnableExtension UnboxedTuples], decls = [DeclValue (FunctionBind "x" [Match {headForm = Prefix, rhs = UnguardedRhs (ETupleUnboxed [EInt 1 TInteger])}]), DeclValue (FunctionBind "f" [Match {headForm = Prefix, pats = [PTupleUnboxed [PVar "y"]], rhs = UnguardedRhs (EVar "y")}])]}
status: pass
