extensions: [DataKinds, FlexibleInstances, TypeOperators]
input: |
  {-# LANGUAGE DataKinds #-}
  {-# LANGUAGE FlexibleInstances #-}
  {-# LANGUAGE TypeOperators #-}
  module M where
  class Infix a b
  instance '() `Infix` ()
ast: |
  Module {name = "M", languagePragmas = [EnableExtension DataKinds, EnableExtension FlexibleInstances, EnableExtension TypeOperators], decls = [DeclClass (ClassDecl {headForm = TypeHeadPrefix, name = "Infix", params = [TyVarBinder {name = "a"}, TyVarBinder {name = "b"}]}), DeclInstance (InstanceDecl {head = TInfix (TTuplePromoted []) "Infix" (TTuple [])})]}
status: pass
