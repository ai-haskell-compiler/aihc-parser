extensions: []
input: |
  module GuardExpressionTypeSignature where

  f :: Bool -> Bool
  f x
    | x :: Bool = True
    | otherwise = False
ast: Module {name = "GuardExpressionTypeSignature", decls = [DeclTypeSig {names = ["f"], type = TFun ArrowUnrestricted (TCon "Bool") (TCon "Bool")}, DeclValue (FunctionBind "f" [Match {headForm = Prefix, pats = [PVar "x"], rhs = GuardedRhss [GuardedRhs {guards = [GuardExpr (ETypeSig (EVar "x") (TCon "Bool"))], body = EVar "True"}, GuardedRhs {guards = [GuardExpr (EVar "otherwise")], body = EVar "False"}]}])]}
status: pass
