extensions: [TemplateHaskell]
input: |
  {-# LANGUAGE TemplateHaskell #-}
  module TH_Operator_Splice where

  x = $(&&)
  y = $(+)
ast: Module {name = "TH_Operator_Splice", languagePragmas = [EnableExtension TemplateHaskell], decls = [DeclValue (FunctionBind "x" [Match {headForm = Prefix, rhs = UnguardedRhs (ETHSplice (EVar "&&"))}]), DeclValue (FunctionBind "y" [Match {headForm = Prefix, rhs = UnguardedRhs (ETHSplice (EVar "+"))}])]}
status: pass
