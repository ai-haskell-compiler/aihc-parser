extensions: [DataKinds, TypeFamilies]
input: |
  {-# LANGUAGE DataKinds #-}
  {-# LANGUAGE TypeFamilies #-}
  module M where
  type family T a
  type instance T '() = ()
ast: |
  Module {name = "M", languagePragmas = [EnableExtension DataKinds, EnableExtension TypeFamilies], decls = [DeclTypeFamilyDecl (TypeFamilyDecl {headForm = TypeHeadPrefix, head = TCon "T", params = [TyVarBinder {name = "a"}]}), DeclTypeFamilyInst (TypeFamilyInst {headForm = TypeHeadPrefix, lhs = TApp (TCon "T") (TTuplePromoted []), rhs = TTuple []})]}
status: pass
